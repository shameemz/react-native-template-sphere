import baseStyles from './../Com/styles';

const styles = {
  ...baseStyles,
  header: {
    ...baseStyles.header,
    backgroundColor: '#808080', // just to show how you can inherit and change the styling.
  },
};

export default styles;
